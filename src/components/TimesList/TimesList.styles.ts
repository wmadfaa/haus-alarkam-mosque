import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flexGrow: 1,
    paddingVertical: 16,
  },
  indicator: {
    alignItems: 'center',
    justifyContent: 'flex-start',
  },
});
